export interface IManager {
  contacts: object;
  dates: object;
  department: string;
  email: string;
  firstName: string;
  lastName: string;
  manager: string;
  phone: string;
  position: string;
  reset_password_expires: string;
  reset_password_token: string;
}

